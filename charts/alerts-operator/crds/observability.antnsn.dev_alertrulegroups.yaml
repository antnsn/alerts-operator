---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: alertrulegroups.observability.antnsn.dev
spec:
  group: observability.antnsn.dev
  names:
    kind: AlertRuleGroup
    listKind: AlertRuleGroupList
    plural: alertrulegroups
    singular: alertrulegroup
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.tenantRef
      name: Tenant
      type: string
    - jsonPath: .spec.backend
      name: Backend
      type: string
    - jsonPath: .status.conditions[?(@.type=="Accepted")].status
      name: Accepted
      type: string
    - jsonPath: .status.conditions[?(@.type=="Synced")].status
      name: Synced
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AlertRuleGroup is the Schema for the alertrulegroups API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              AlertRuleGroupSpec defines the desired state of AlertRuleGroup.
              Group-name uniqueness is enforced by +listType=map on Groups (no CEL needed).
            properties:
              backend:
                description: Backend specifies which backend (Mimir or Loki) to use
                  for alert rules.
                enum:
                - mimir
                - loki
                type: string
              groups:
                items:
                  description: RuleGroup is a collection of alert and recording rules
                    grouped by name.
                  properties:
                    interval:
                      type: string
                    name:
                      minLength: 1
                      type: string
                    rules:
                      items:
                        description: Rule is one alerting or recording rule (PrometheusRule-compatible
                          shape).
                        properties:
                          alert:
                            minLength: 1
                            type: string
                          annotations:
                            additionalProperties:
                              type: string
                            type: object
                          expr:
                            description: 'Expr must be a YAML string. PrometheusRule
                              allows a bare number (IntOrString); quote it here, e.g.
                              expr: "1".'
                            minLength: 1
                            type: string
                          for:
                            type: string
                          keep_firing_for:
                            type: string
                          labels:
                            additionalProperties:
                              type: string
                            type: object
                          record:
                            minLength: 1
                            type: string
                        required:
                        - expr
                        type: object
                        x-kubernetes-validations:
                        - message: exactly one of alert or record must be set
                          rule: has(self.alert) != has(self.record)
                      minItems: 1
                      type: array
                  required:
                  - name
                  - rules
                  type: object
                minItems: 1
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              tenantRef:
                description: TenantRef is the name of the cluster-scoped Tenant.
                minLength: 1
                type: string
            required:
            - backend
            - groups
            - tenantRef
            type: object
          status:
            description: AlertRuleGroupStatus defines the observed state of AlertRuleGroup.
            properties:
              backendNamespace:
                description: BackendNamespace is the rule namespace used in the backend,
                  <prefix>/<namespace>/<name>.
                type: string
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
