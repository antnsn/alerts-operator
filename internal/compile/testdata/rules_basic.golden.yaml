alerts-operator/monitoring/homelab:
- interval: 1m
  name: node.health
  rules:
  - alert: NodeDown
    annotations:
      summary: '{{ $labels.instance }} down'
    expr: up{job="node"} == 0
    for: 5m
    labels:
      severity: critical
  - expr: avg by (job) (up)
    record: job:up:ratio
alerts-operator_loki_udm:
- name: udm
  rules:
  - alert: UDMErrors
    expr: sum(rate({host="udm"} |= "error" [5m])) > 1
    keep_firing_for: 10m
