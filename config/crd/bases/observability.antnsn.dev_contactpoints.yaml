---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: contactpoints.observability.antnsn.dev
spec:
  group: observability.antnsn.dev
  names:
    kind: ContactPoint
    listKind: ContactPointList
    plural: contactpoints
    singular: contactpoint
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.tenantRef
      name: Tenant
      type: string
    - jsonPath: .status.conditions[?(@.type=="Accepted")].status
      name: Accepted
      type: string
    - jsonPath: .status.conditions[?(@.type=="Synced")].status
      name: Synced
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ContactPoint is the Schema for the contactpoints API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ContactPointSpec mirrors Alertmanager receiver configuration.
              Secrets only via secretKeyRef.
            properties:
              discord:
                items:
                  description: DiscordConfig specifies Discord notification settings.
                  properties:
                    message:
                      type: string
                    sendResolved:
                      type: boolean
                    title:
                      type: string
                    webhookURLSecretRef:
                      description: SecretKeyRef points at one key of a Secret in the
                        same namespace as the referencing object.
                      properties:
                        key:
                          minLength: 1
                          type: string
                        name:
                          minLength: 1
                          type: string
                      required:
                      - key
                      - name
                      type: object
                  required:
                  - webhookURLSecretRef
                  type: object
                type: array
              email:
                items:
                  description: EmailConfig specifies email notification settings.
                  properties:
                    authPasswordSecretRef:
                      description: SecretKeyRef points at one key of a Secret in the
                        same namespace as the referencing object.
                      properties:
                        key:
                          minLength: 1
                          type: string
                        name:
                          minLength: 1
                          type: string
                      required:
                      - key
                      - name
                      type: object
                    authUsername:
                      type: string
                    from:
                      type: string
                    hello:
                      type: string
                    requireTLS:
                      type: boolean
                    sendResolved:
                      type: boolean
                    smarthost:
                      type: string
                    to:
                      minLength: 1
                      type: string
                  required:
                  - to
                  type: object
                type: array
              pushover:
                items:
                  description: PushoverConfig specifies Pushover notification settings.
                  properties:
                    message:
                      type: string
                    priority:
                      type: string
                    sendResolved:
                      type: boolean
                    sound:
                      type: string
                    title:
                      type: string
                    tokenSecretRef:
                      description: SecretKeyRef points at one key of a Secret in the
                        same namespace as the referencing object.
                      properties:
                        key:
                          minLength: 1
                          type: string
                        name:
                          minLength: 1
                          type: string
                      required:
                      - key
                      - name
                      type: object
                    url:
                      type: string
                    urlTitle:
                      type: string
                    userKeySecretRef:
                      description: SecretKeyRef points at one key of a Secret in the
                        same namespace as the referencing object.
                      properties:
                        key:
                          minLength: 1
                          type: string
                        name:
                          minLength: 1
                          type: string
                      required:
                      - key
                      - name
                      type: object
                  required:
                  - tokenSecretRef
                  - userKeySecretRef
                  type: object
                type: array
              slack:
                items:
                  description: SlackConfig specifies Slack notification settings.
                  properties:
                    apiURLSecretRef:
                      description: SecretKeyRef points at one key of a Secret in the
                        same namespace as the referencing object.
                      properties:
                        key:
                          minLength: 1
                          type: string
                        name:
                          minLength: 1
                          type: string
                      required:
                      - key
                      - name
                      type: object
                    channel:
                      type: string
                    iconEmoji:
                      type: string
                    sendResolved:
                      type: boolean
                    text:
                      type: string
                    title:
                      type: string
                    username:
                      type: string
                  required:
                  - apiURLSecretRef
                  type: object
                type: array
              telegram:
                items:
                  description: TelegramConfig specifies Telegram notification settings.
                  properties:
                    botTokenSecretRef:
                      description: SecretKeyRef points at one key of a Secret in the
                        same namespace as the referencing object.
                      properties:
                        key:
                          minLength: 1
                          type: string
                        name:
                          minLength: 1
                          type: string
                      required:
                      - key
                      - name
                      type: object
                    chatID:
                      format: int64
                      type: integer
                    message:
                      type: string
                    parseMode:
                      enum:
                      - MarkdownV2
                      - Markdown
                      - HTML
                      - ""
                      type: string
                    sendResolved:
                      type: boolean
                  required:
                  - botTokenSecretRef
                  - chatID
                  type: object
                type: array
              tenantRef:
                minLength: 1
                type: string
              webhook:
                items:
                  description: WebhookConfig specifies webhook notification settings
                    with URL and HTTP authentication.
                  properties:
                    httpConfig:
                      description: HTTPConfig specifies HTTP authentication options
                        for webhook notifications.
                      properties:
                        basicAuth:
                          description: HTTPBasicAuth specifies username and password
                            for HTTP basic authentication.
                          properties:
                            passwordSecretRef:
                              description: SecretKeyRef points at one key of a Secret
                                in the same namespace as the referencing object.
                              properties:
                                key:
                                  minLength: 1
                                  type: string
                                name:
                                  minLength: 1
                                  type: string
                              required:
                              - key
                              - name
                              type: object
                            usernameSecretRef:
                              description: SecretKeyRef points at one key of a Secret
                                in the same namespace as the referencing object.
                              properties:
                                key:
                                  minLength: 1
                                  type: string
                                name:
                                  minLength: 1
                                  type: string
                              required:
                              - key
                              - name
                              type: object
                          required:
                          - passwordSecretRef
                          - usernameSecretRef
                          type: object
                        bearerTokenSecretRef:
                          description: SecretKeyRef points at one key of a Secret
                            in the same namespace as the referencing object.
                          properties:
                            key:
                              minLength: 1
                              type: string
                            name:
                              minLength: 1
                              type: string
                          required:
                          - key
                          - name
                          type: object
                      type: object
                    maxAlerts:
                      format: int32
                      type: integer
                    sendResolved:
                      type: boolean
                    url:
                      minLength: 1
                      type: string
                    urlSecretRef:
                      description: SecretKeyRef points at one key of a Secret in the
                        same namespace as the referencing object.
                      properties:
                        key:
                          minLength: 1
                          type: string
                        name:
                          minLength: 1
                          type: string
                      required:
                      - key
                      - name
                      type: object
                  type: object
                  x-kubernetes-validations:
                  - message: exactly one of url or urlSecretRef is required
                    rule: has(self.url) != has(self.urlSecretRef)
                type: array
            required:
            - tenantRef
            type: object
            x-kubernetes-validations:
            - message: at least one receiver configuration is required
              rule: (has(self.webhook) && self.webhook.size() > 0) || (has(self.pushover)
                && self.pushover.size() > 0) || (has(self.slack) && self.slack.size()
                > 0) || (has(self.discord) && self.discord.size() > 0) || (has(self.telegram)
                && self.telegram.size() > 0) || (has(self.email) && self.email.size()
                > 0)
          status:
            description: ContactPointStatus defines the observed state of ContactPoint.
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
